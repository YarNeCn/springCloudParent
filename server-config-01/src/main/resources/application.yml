
spring:
  application:
    name: server-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YarNeCn/springCloudConfigTest
          username:
          password:
          search-paths: repCon
          default-label: master
server:
  port: 8085
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/

